import { SearchInputValidationDirective } from './search-input-validation.directive';

describe('SearchInputValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchInputValidationDirective();
    expect(directive).toBeTruthy();
  });
});
